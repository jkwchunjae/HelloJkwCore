@inherits JkwPageBase
@page "/diary/edit/{DiaryName}/{Date}"

@if (HasDiaryContent)
{
    <MudGrid>
        <MudItem>
            <MudText Typo="Typo.h4">
                @View.DiaryContents[0].Date.ToString("yyyy.MM.dd dddd")
            </MudText>
        </MudItem>
    </MudGrid>
    <MudGrid>
        @foreach (var content in View.DiaryContents)
        {
        <MudItem Style="width: 100%">
            <MudTextField T="string" @bind-Value="content.Text" Lines="15" Style="line-height: 30px;"/>
        </MudItem>
        }
    </MudGrid>
    <MudGrid>
        <MudItem>
            <MudButton OnClick="EditDiary" StartIcon="@Icons.Material.Filled.Save" Variant="Variant.Filled" Color="Color.Info">
                수정
            </MudButton>
        </MudItem>
    </MudGrid>
}