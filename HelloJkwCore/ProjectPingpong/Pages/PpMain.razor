@inherits JkwPageBase
@layout PpLayout
@page "/jangtak9"
@using MudBlazor
@using ProjectPingpong.Pages.Competition

<PpCompetitionList/>

@if (IsAuthenticated && (User?.HasRole(UserRole.Admin) ?? false))
{
  <PpCompetitionCreation/>
}

