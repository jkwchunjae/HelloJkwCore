
@if (User != null)
{
  if (Icon != null)
  {
    <MudIcon Icon="Icon"/>
  }
  <MudElement HtmlTag="span">
    @User.DisplayName
  </MudElement>

  if (OnDelete.HasDelegate)
  {
    <MudButton OnClick="DeleteButtonClicked">삭제</MudButton>
  }
}
